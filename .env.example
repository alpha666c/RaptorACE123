# Placeholders only. Real keys live in VS Code SecretStorage / macOS keychain.
# This file is committed; never put actual secrets here.
OPENROUTER_API_KEY=sk-or-v1-REPLACE_IN_SECRETSTORAGE
AGENT_READ_ONLY=0
